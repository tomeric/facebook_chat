!!!
%html
  %head
    %meta{ charset: 'utf-8' }/
    :css
      body { background: #d6dde4; font-family: "Lucida Grande"; font-size: 12px; margin: 0 }
      ul { margin: 0; padding: 0 }
      a {
        color: #000;
        cursor: default;
        display: block;
        padding: 4px 10px;
        text-decoration: none;
      }
      
      a:active {
        background-image: -webkit-linear-gradient(top, #5EADE4 0, #3587D4 100%);
        border-bottom: #2C78CD 1px solid;
        border-top: #509FDD 1px solid;
        box-shadow: inset 0 1px rgba(255, 255, 255, 0.2);
        color: #fff;
        font-weight: bold;
        padding: 3px 10px;
        text-shadow: 0 1px rgba(0, 0, 0, 0.15);
      }
      .avatar {
        background: #fff;
        border-radius: 2px;
        box-shadow: 0 1px rgba(0, 0, 0, 0.1);
        float: left;
        margin-right: 10px;
        height: 24px;
        width: 24px;
      }
      
      .name {
        display: block;
        line-height: 24px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
  %body
    %ul
      - contacts.each do |contact|
        %li
          %a(href="#")
            %span.avatar
            %span.name= contact
